services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    ports:
      - "8080:80"
    restart: unless-stopped

  apk:
    build:
      context: .
      dockerfile: Dockerfile.android
    volumes:
      - ./docker-output:/output
    profiles:
      - build
